fun get_string() string {
    let a = "some cool "
    let b = "string! 🥹"
    return a^b
}

fun concat(a string b string) {
    return a ^ b
}

let s = get_string() ^ "\nconcat " ^ "all the things"
print s
return 0
